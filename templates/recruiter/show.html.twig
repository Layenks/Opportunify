{% extends 'base.html.twig' %}

{% block title %}Recruiter{% endblock %}

{% block body %}
    <h1>Recruiter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recruiter.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ recruiter.username }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ recruiter.roles ? recruiter.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ recruiter.password }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ recruiter.position }}</td>
            </tr>
            <tr>
                <th>EnterpriseName</th>
                <td>{{ recruiter.enterpriseName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recruiter_index') }}">back to list</a>

    <a href="{{ path('app_recruiter_edit', {'id': recruiter.id}) }}">edit</a>

    {{ include('recruiter/_delete_form.html.twig') }}
{% endblock %}
